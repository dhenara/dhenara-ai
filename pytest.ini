[pytest]
markers =
    unit: Single function/class, all deps stubbed
    component: Several classes within one package
    service: FastAPI route, CLI cmd, external deps mocked
    integration: Real services talking together (DB, LLM fake)
    e2e: Full user journey, Docker/Flutter
    case_id(id): link to TESTPLAN
